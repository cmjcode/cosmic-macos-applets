[package]
name = "egui-global-menu"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Example: export an egui/eframe menu bar to a desktop global menu"
publish = false

[dependencies]
eframe = "0.36"
serde = { version = "1", features = ["derive"] }
zbus = "5"
